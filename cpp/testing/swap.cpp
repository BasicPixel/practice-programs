/* C++ Program to swap the values for two variables. */

#include <iostream>

void swap(int *x, int *y)
{
}

int main()
{
    int a, b;

    a = 15, b = 10;

    printf("a = %i, b = %i\n", a, b);

    swap();

    printf("a = %i, b = %i\n", a, b);

    return 0;
}
